#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

void solve()
{
    ll a, n, n_a;
    cin >> a >> n;
    n_a = ceil(log10(a));
    vector<ll> d(n);
    for (ll i = 0; i < n; i++)
    {
        cin >> d[i];
    }
    ll minol = a;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }
    return 0;
}