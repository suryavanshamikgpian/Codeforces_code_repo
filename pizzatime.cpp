#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
signed main()

{
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#else
#endif
    ll t;
    cin >> t;
    while (t--)
    {
        ll n;
        cin >> n;
        ll h = 0;
        ll a = 0;
        h = ceil((double)(n - 2) / 2);

        cout << h << endl;
    }
}