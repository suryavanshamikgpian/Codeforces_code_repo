#include <bits/stdc++.h>
typedef long long ll;

using namespace std;

void solve()
{
    ll n, x, s;
    cin >> n >> x >> s;
    string str;
    cin >> str;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}